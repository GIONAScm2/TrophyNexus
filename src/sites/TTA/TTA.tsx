import {useEffect} from 'preact/hooks';
import { TrophyNexusTTA } from './nexus';

interface TTAProps {
	nexus: TrophyNexusTTA;
}

export const TTA: preact.FunctionComponent<TTAProps> = ({children, nexus}) => {
	useEffect(() => {
		(async () => {
			//
		})();
	}, []);


	return <></>;
};
